# Test configuration - uses Testcontainers MySQL
# Database properties are set dynamically by @DynamicPropertySource

spring.jpa.hibernate.ddl-auto=create-drop

# Disable Eureka for tests
eureka.client.enabled=false
