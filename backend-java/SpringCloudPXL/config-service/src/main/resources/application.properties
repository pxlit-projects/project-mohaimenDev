server.port=8888
spring.application.name=config-service

# Use native profile (local filesystem instead of Git)
spring.profiles.active=native

# Location of config files - use file: prefix for absolute path
spring.cloud.config.server.native.search-locations=file:./config-service/src/main/resources/config
